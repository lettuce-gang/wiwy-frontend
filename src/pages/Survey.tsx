import React from 'react';
import ProgressBar from "../components/ProgressBar";

function Survey() {
    return (
        <div>
            <ProgressBar />
        </div>
    );
}

export default Survey;